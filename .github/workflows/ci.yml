name: CI

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  quality:
    name: Quality (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Restore Cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "24"
          cache: true
          run-install: true

      - name: Check JS and TS
        run: vp check

      - name: Check Rust formatting
        run: vp run -w fmt_check_rust

      - name: Lint Rust
        run: vp run -w lint_rust

      - name: Build
        run: vp run -w build_ci

      - name: Test
        run: vp run -w test

      - name: Validate public facade without experimental features
        run: cargo test -p corsa_bind_rs --no-default-features --test orchestrator

  real-corsa-smoke:
    name: Real Corsa Smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Setup Go for pinned upstream ref
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.0"
          cache: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "24"
          cache: true
          run-install: true

      - name: Sync pinned upstream checkout
        run: vp run -w sync_origin

      - name: Verify pinned upstream checkout
        run: vp run -w verify_origin

      - name: Build pinned Corsa binary
        run: vp run -w build_corsa

      - name: Real Corsa smoke tests
        run: cargo test -p corsa_bind_rs --no-default-features --test real_corsa_regression --test real_corsa_typecheck

      - name: Real examples
        run: vp run -w examples_real

  bench-corsa-origin:
    name: Corsa Origin Bench
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Cargo cache
        uses: Swatinem/rust-cache@v2

      - name: Setup Go for pinned upstream ref
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.0"
          cache: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "24"
          cache: true
          run-install: true

      - name: Sync pinned upstream checkout
        run: vp run -w sync_origin

      - name: Verify pinned upstream checkout
        run: vp run -w verify_origin

      - name: Build pinned Corsa binary
        run: vp run -w build_corsa

      - name: Real Corsa baseline and regression tests
        run: cargo test -p corsa_bind_rs --no-default-features --test real_corsa_baseline --test real_corsa_regression --test real_corsa_typecheck

      - name: Benchmark guards
        run: vp run -w bench_verify
